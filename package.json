{
  "name": "ecommerce-backend-vercel",
  "version": "1.0.0",
  "description": "Backend serverless function for email sending via Resend.",
  "main": "api/send-email.js",
  "scripts": {
    "start": "echo 'Rodando apenas a função serverless, sem servidor web completo.'"
  },
  "dependencies": {
    "resend": "^3.5.1" 
    /* Use a versão estável mais recente */
  }
}